Title: Post Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Post controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Post controller** is a front-end helper that wraps the TRPC router for the `post` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/post.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/post.ts`

Custom overrides (project side):
- `schema/controllers/post.ts` (or `app/schema/controllers/post.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedPostController()`
- `extendPostController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { PostController } from '@schema/controllers/post'

export const extendPostController = (base: PostController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'p', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
None

Optional fields:
- contentTable
- contentId
- createdAt
- updatedAt
- publishedAt
- title
- permalink
- status

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `post.create`

### update(id, payload)
- TRPC endpoint: `post.update`

### delete(id)
- TRPC endpoint: `post.delete`

### get(id, resourceKey)
- TRPC endpoint: `post.resource`
- Known resource keys: None

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `post.<taxonomy>.createTaxonomy`
- `post.<taxonomy>.addTerm`
- `post.<taxonomy>.removeTerm`
- `post.<taxonomy>.attach`
- `post.<taxonomy>.detach`
- `post.<taxonomy>.getTerms`
- `post.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `post.subtables.<child>.create`
- `post.subtables.<child>.update`
- `post.subtables.<child>.delete`
- `post.subtables.<child>.get`
- `post.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `post.relations.<relation>.attach`
- `post.relations.<relation>.detach`
- `post.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `post.typesense.resource`
- `post.typesense.list`
- `post.typesense.refresh`
- `post.typesense.count`
- `post.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/post.ts`

Wrapper router:
- `server/trpc/routers/post.ts`

---

End of document.
