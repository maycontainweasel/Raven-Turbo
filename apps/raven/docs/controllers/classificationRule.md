Title: ClassificationRule Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **ClassificationRule controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **ClassificationRule controller** is a front-end helper that wraps the TRPC router for the `classificationRule` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/classificationRule.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/classificationRule.ts`

Custom overrides (project side):
- `schema/controllers/classificationRule.ts` (or `app/schema/controllers/classificationRule.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedClassificationRuleController()`
- `extendClassificationRuleController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { ClassificationRuleController } from '@schema/controllers/classificationRule'

export const extendClassificationRuleController = (base: ClassificationRuleController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'classificationRule', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- key (required)
- name (required)
- matcher (required)
- scope (required)
- active (required)

Optional fields:
- targetMerchant
- targetCategory
- priority
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `classificationRule.create`

### update(id, payload)
- TRPC endpoint: `classificationRule.update`

### delete(id)
- TRPC endpoint: `classificationRule.delete`

### get(id, resourceKey)
- TRPC endpoint: `classificationRule.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `classificationRule.<taxonomy>.createTaxonomy`
- `classificationRule.<taxonomy>.addTerm`
- `classificationRule.<taxonomy>.removeTerm`
- `classificationRule.<taxonomy>.attach`
- `classificationRule.<taxonomy>.detach`
- `classificationRule.<taxonomy>.getTerms`
- `classificationRule.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `classificationRule.subtables.<child>.create`
- `classificationRule.subtables.<child>.update`
- `classificationRule.subtables.<child>.delete`
- `classificationRule.subtables.<child>.get`
- `classificationRule.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `classificationRule.relations.<relation>.attach`
- `classificationRule.relations.<relation>.detach`
- `classificationRule.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `classificationRule.typesense.resource`
- `classificationRule.typesense.list`
- `classificationRule.typesense.refresh`
- `classificationRule.typesense.count`
- `classificationRule.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/classificationRule.ts`

Wrapper router:
- `server/trpc/routers/classificationRule.ts`

---

End of document.
