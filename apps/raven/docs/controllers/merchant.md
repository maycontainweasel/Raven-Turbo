Title: Merchant Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Merchant controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Merchant controller** is a front-end helper that wraps the TRPC router for the `merchant` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/merchant.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/merchant.ts`

Custom overrides (project side):
- `schema/controllers/merchant.ts` (or `app/schema/controllers/merchant.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedMerchantController()`
- `extendMerchantController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { MerchantController } from '@schema/controllers/merchant'

export const extendMerchantController = (base: MerchantController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'merchant', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- key (required)
- name (required)
- active (required)

Optional fields:
- normalizedName
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `merchant.create`

### update(id, payload)
- TRPC endpoint: `merchant.update`

### delete(id)
- TRPC endpoint: `merchant.delete`

### get(id, resourceKey)
- TRPC endpoint: `merchant.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `merchant.<taxonomy>.createTaxonomy`
- `merchant.<taxonomy>.addTerm`
- `merchant.<taxonomy>.removeTerm`
- `merchant.<taxonomy>.attach`
- `merchant.<taxonomy>.detach`
- `merchant.<taxonomy>.getTerms`
- `merchant.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `merchant.subtables.<child>.create`
- `merchant.subtables.<child>.update`
- `merchant.subtables.<child>.delete`
- `merchant.subtables.<child>.get`
- `merchant.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `merchant.relations.<relation>.attach`
- `merchant.relations.<relation>.detach`
- `merchant.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `merchant.typesense.resource`
- `merchant.typesense.list`
- `merchant.typesense.refresh`
- `merchant.typesense.count`
- `merchant.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/merchant.ts`

Wrapper router:
- `server/trpc/routers/merchant.ts`

---

End of document.
