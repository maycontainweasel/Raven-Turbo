Title: Transaction Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Transaction controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Transaction controller** is a front-end helper that wraps the TRPC router for the `transaction` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/transaction.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/transaction.ts`

Custom overrides (project side):
- `schema/controllers/transaction.ts` (or `app/schema/controllers/transaction.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedTransactionController()`
- `extendTransactionController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { TransactionController } from '@schema/controllers/transaction'

export const extendTransactionController = (base: TransactionController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'transaction', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- account (required)
- key (required)
- postedAt (required)
- description (required)
- amount (required)
- currency (required)
- direction (required)
- status (required)

Optional fields:
- sourceRow
- effectiveAt
- normalizedDescription
- balance
- confidence
- merchantName
- merchant
- category
- isTransfer
- manualOverride
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `transaction.create`

### update(id, payload)
- TRPC endpoint: `transaction.update`

### delete(id)
- TRPC endpoint: `transaction.delete`

### get(id, resourceKey)
- TRPC endpoint: `transaction.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `transaction.<taxonomy>.createTaxonomy`
- `transaction.<taxonomy>.addTerm`
- `transaction.<taxonomy>.removeTerm`
- `transaction.<taxonomy>.attach`
- `transaction.<taxonomy>.detach`
- `transaction.<taxonomy>.getTerms`
- `transaction.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `transaction.subtables.<child>.create`
- `transaction.subtables.<child>.update`
- `transaction.subtables.<child>.delete`
- `transaction.subtables.<child>.get`
- `transaction.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `transaction.relations.<relation>.attach`
- `transaction.relations.<relation>.detach`
- `transaction.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `transaction.typesense.resource`
- `transaction.typesense.list`
- `transaction.typesense.refresh`
- `transaction.typesense.count`
- `transaction.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/transaction.ts`

Wrapper router:
- `server/trpc/routers/transaction.ts`

---

End of document.
