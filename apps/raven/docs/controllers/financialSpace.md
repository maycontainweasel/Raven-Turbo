Title: FinancialSpace Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **FinancialSpace controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **FinancialSpace controller** is a front-end helper that wraps the TRPC router for the `financialSpace` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/financialSpace.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/financialSpace.ts`

Custom overrides (project side):
- `schema/controllers/financialSpace.ts` (or `app/schema/controllers/financialSpace.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedFinancialSpaceController()`
- `extendFinancialSpaceController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { FinancialSpaceController } from '@schema/controllers/financialSpace'

export const extendFinancialSpaceController = (base: FinancialSpaceController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'financialSpace', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- key (required)
- name (required)
- spaceType (required)
- currency (required)
- active (required)

Optional fields:
- description
- archivedAt

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `financialSpace.create`

### update(id, payload)
- TRPC endpoint: `financialSpace.update`

### delete(id)
- TRPC endpoint: `financialSpace.delete`

### get(id, resourceKey)
- TRPC endpoint: `financialSpace.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `financialSpace.<taxonomy>.createTaxonomy`
- `financialSpace.<taxonomy>.addTerm`
- `financialSpace.<taxonomy>.removeTerm`
- `financialSpace.<taxonomy>.attach`
- `financialSpace.<taxonomy>.detach`
- `financialSpace.<taxonomy>.getTerms`
- `financialSpace.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `financialSpace.subtables.<child>.create`
- `financialSpace.subtables.<child>.update`
- `financialSpace.subtables.<child>.delete`
- `financialSpace.subtables.<child>.get`
- `financialSpace.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `financialSpace.relations.<relation>.attach`
- `financialSpace.relations.<relation>.detach`
- `financialSpace.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `financialSpace.typesense.resource`
- `financialSpace.typesense.list`
- `financialSpace.typesense.refresh`
- `financialSpace.typesense.count`
- `financialSpace.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/financialSpace.ts`

Wrapper router:
- `server/trpc/routers/financialSpace.ts`

---

End of document.
