Title: Account Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Account controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Account controller** is a front-end helper that wraps the TRPC router for the `account` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/account.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/account.ts`

Custom overrides (project side):
- `schema/controllers/account.ts` (or `app/schema/controllers/account.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedAccountController()`
- `extendAccountController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { AccountController } from '@schema/controllers/account'

export const extendAccountController = (base: AccountController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'account', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- key (required)
- name (required)
- bankName (required)
- accountType (required)
- currency (required)
- active (required)

Optional fields:
- accountNumberLast4
- openingBalance
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `account.create`

### update(id, payload)
- TRPC endpoint: `account.update`

### delete(id)
- TRPC endpoint: `account.delete`

### get(id, resourceKey)
- TRPC endpoint: `account.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `account.<taxonomy>.createTaxonomy`
- `account.<taxonomy>.addTerm`
- `account.<taxonomy>.removeTerm`
- `account.<taxonomy>.attach`
- `account.<taxonomy>.detach`
- `account.<taxonomy>.getTerms`
- `account.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `account.subtables.<child>.create`
- `account.subtables.<child>.update`
- `account.subtables.<child>.delete`
- `account.subtables.<child>.get`
- `account.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `account.relations.<relation>.attach`
- `account.relations.<relation>.detach`
- `account.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `account.typesense.resource`
- `account.typesense.list`
- `account.typesense.refresh`
- `account.typesense.count`
- `account.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/account.ts`

Wrapper router:
- `server/trpc/routers/account.ts`

---

End of document.
