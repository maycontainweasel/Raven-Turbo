Title: ClarificationTask Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **ClarificationTask controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **ClarificationTask controller** is a front-end helper that wraps the TRPC router for the `clarificationTask` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/clarificationTask.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/clarificationTask.ts`

Custom overrides (project side):
- `schema/controllers/clarificationTask.ts` (or `app/schema/controllers/clarificationTask.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedClarificationTaskController()`
- `extendClarificationTaskController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { ClarificationTaskController } from '@schema/controllers/clarificationTask'

export const extendClarificationTaskController = (base: ClarificationTaskController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'clarificationTask', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- key (required)
- transaction (required)
- status (required)
- question (required)

Optional fields:
- reason
- suggestedSpace
- suggestedMerchant
- suggestedCategory
- resolvedAt
- resolutionNotes
- confidence
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `clarificationTask.create`

### update(id, payload)
- TRPC endpoint: `clarificationTask.update`

### delete(id)
- TRPC endpoint: `clarificationTask.delete`

### get(id, resourceKey)
- TRPC endpoint: `clarificationTask.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `clarificationTask.<taxonomy>.createTaxonomy`
- `clarificationTask.<taxonomy>.addTerm`
- `clarificationTask.<taxonomy>.removeTerm`
- `clarificationTask.<taxonomy>.attach`
- `clarificationTask.<taxonomy>.detach`
- `clarificationTask.<taxonomy>.getTerms`
- `clarificationTask.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `clarificationTask.subtables.<child>.create`
- `clarificationTask.subtables.<child>.update`
- `clarificationTask.subtables.<child>.delete`
- `clarificationTask.subtables.<child>.get`
- `clarificationTask.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `clarificationTask.relations.<relation>.attach`
- `clarificationTask.relations.<relation>.detach`
- `clarificationTask.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `clarificationTask.typesense.resource`
- `clarificationTask.typesense.list`
- `clarificationTask.typesense.refresh`
- `clarificationTask.typesense.count`
- `clarificationTask.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/clarificationTask.ts`

Wrapper router:
- `server/trpc/routers/clarificationTask.ts`

---

End of document.
