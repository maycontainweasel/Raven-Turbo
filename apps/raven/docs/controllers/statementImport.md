Title: StatementImport Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **StatementImport controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **StatementImport controller** is a front-end helper that wraps the TRPC router for the `statementImport` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/statementImport.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/statementImport.ts`

Custom overrides (project side):
- `schema/controllers/statementImport.ts` (or `app/schema/controllers/statementImport.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedStatementImportController()`
- `extendStatementImportController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { StatementImportController } from '@schema/controllers/statementImport'

export const extendStatementImportController = (base: StatementImportController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'statementImport', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- account (required)
- key (required)
- sourceFileName (required)
- sourceFormat (required)
- importedAt (required)
- status (required)

Optional fields:
- statementDate
- rowCount
- normalizedCount
- clarificationCount
- sourceChecksum
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `statementImport.create`

### update(id, payload)
- TRPC endpoint: `statementImport.update`

### delete(id)
- TRPC endpoint: `statementImport.delete`

### get(id, resourceKey)
- TRPC endpoint: `statementImport.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `statementImport.<taxonomy>.createTaxonomy`
- `statementImport.<taxonomy>.addTerm`
- `statementImport.<taxonomy>.removeTerm`
- `statementImport.<taxonomy>.attach`
- `statementImport.<taxonomy>.detach`
- `statementImport.<taxonomy>.getTerms`
- `statementImport.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `statementImport.subtables.<child>.create`
- `statementImport.subtables.<child>.update`
- `statementImport.subtables.<child>.delete`
- `statementImport.subtables.<child>.get`
- `statementImport.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `statementImport.relations.<relation>.attach`
- `statementImport.relations.<relation>.detach`
- `statementImport.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `statementImport.typesense.resource`
- `statementImport.typesense.list`
- `statementImport.typesense.refresh`
- `statementImport.typesense.count`
- `statementImport.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/statementImport.ts`

Wrapper router:
- `server/trpc/routers/statementImport.ts`

---

End of document.
