Title: Category Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **Category controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **Category controller** is a front-end helper that wraps the TRPC router for the `category` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/category.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/category.ts`

Custom overrides (project side):
- `schema/controllers/category.ts` (or `app/schema/controllers/category.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedCategoryController()`
- `extendCategoryController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { CategoryController } from '@schema/controllers/category'

export const extendCategoryController = (base: CategoryController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'category', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- key (required)
- name (required)
- categoryKind (required)
- active (required)

Optional fields:
- parentCategory
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `category.create`

### update(id, payload)
- TRPC endpoint: `category.update`

### delete(id)
- TRPC endpoint: `category.delete`

### get(id, resourceKey)
- TRPC endpoint: `category.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `category.<taxonomy>.createTaxonomy`
- `category.<taxonomy>.addTerm`
- `category.<taxonomy>.removeTerm`
- `category.<taxonomy>.attach`
- `category.<taxonomy>.detach`
- `category.<taxonomy>.getTerms`
- `category.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `category.subtables.<child>.create`
- `category.subtables.<child>.update`
- `category.subtables.<child>.delete`
- `category.subtables.<child>.get`
- `category.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `category.relations.<relation>.attach`
- `category.relations.<relation>.detach`
- `category.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `category.typesense.resource`
- `category.typesense.list`
- `category.typesense.refresh`
- `category.typesense.count`
- `category.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/category.ts`

Wrapper router:
- `server/trpc/routers/category.ts`

---

End of document.
