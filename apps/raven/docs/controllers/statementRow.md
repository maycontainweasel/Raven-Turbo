Title: StatementRow Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **StatementRow controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **StatementRow controller** is a front-end helper that wraps the TRPC router for the `statementRow` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/statementRow.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/statementRow.ts`

Custom overrides (project side):
- `schema/controllers/statementRow.ts` (or `app/schema/controllers/statementRow.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedStatementRowController()`
- `extendStatementRowController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { StatementRowController } from '@schema/controllers/statementRow'

export const extendStatementRowController = (base: StatementRowController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'statementRow', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- account (required)
- statementImport (required)
- rowNumber (required)
- description (required)
- amount (required)
- currency (required)
- direction (required)
- status (required)

Optional fields:
- postedAt
- normalizedDescription
- balance
- confidence
- rawData
- transaction
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `statementRow.create`

### update(id, payload)
- TRPC endpoint: `statementRow.update`

### delete(id)
- TRPC endpoint: `statementRow.delete`

### get(id, resourceKey)
- TRPC endpoint: `statementRow.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `statementRow.<taxonomy>.createTaxonomy`
- `statementRow.<taxonomy>.addTerm`
- `statementRow.<taxonomy>.removeTerm`
- `statementRow.<taxonomy>.attach`
- `statementRow.<taxonomy>.detach`
- `statementRow.<taxonomy>.getTerms`
- `statementRow.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `statementRow.subtables.<child>.create`
- `statementRow.subtables.<child>.update`
- `statementRow.subtables.<child>.delete`
- `statementRow.subtables.<child>.get`
- `statementRow.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `statementRow.relations.<relation>.attach`
- `statementRow.relations.<relation>.detach`
- `statementRow.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `statementRow.typesense.resource`
- `statementRow.typesense.list`
- `statementRow.typesense.refresh`
- `statementRow.typesense.count`
- `statementRow.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/statementRow.ts`

Wrapper router:
- `server/trpc/routers/statementRow.ts`

---

End of document.
