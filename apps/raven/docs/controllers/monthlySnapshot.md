Title: MonthlySnapshot Controller (Schema Kit)
Scope: schema-kit module
Applies to: Nuxt 4 + TRPC + SurrealDB

This document explains the **MonthlySnapshot controller** generated by Schema Kit.
It is intentionally explicit so an AI can follow it without confusion.

---

## 1) What this controller is

The **MonthlySnapshot controller** is a front-end helper that wraps the TRPC router for the `monthlySnapshot` model.
It gives you simple methods like `create`, `update`, `delete`, `get`, `taxonomy`, `subtable`, `relation`, and `typesense` (when enabled).

---

## 2) Where the controller lives

Generated base controller:
- `module/src/runtime/controllers/generated/monthlySnapshot.ts`

Wrapper that merges generated + custom overrides:
- `module/src/runtime/controllers/monthlySnapshot.ts`

Custom overrides (project side):
- `schema/controllers/monthlySnapshot.ts` (or `app/schema/controllers/monthlySnapshot.ts`)

---

## 3) How overrides work

The wrapper calls:
- `createGeneratedMonthlySnapshotController()`
- `extendMonthlySnapshotController` (your custom overrides)

If a custom function has the **same name** as a generated function, the custom one **replaces** it.
If it’s a new function, it is added.

Example override:

```ts
import type { MonthlySnapshotController } from '@schema/controllers/monthlySnapshot'

export const extendMonthlySnapshotController = (base: MonthlySnapshotController) => ({
  async update(id, payload, options) {
    return base.update(id, payload, options)
  },
  async customThing(id) {
    return base.get(id, 'Admin')
  },
})
```

---

## 4) ID normalization (very important)

SurrealDB returns record ids as **objects** like:
```ts
{ tb: 'monthlySnapshot', id: '123' }
```

TRPC endpoints expect the **sub-id**, which for this model is:
`string`

The controller accepts **either**:
- a record object
- a sub-id
- a full record object
- arrays/tuples of ids

It always normalizes to the sub-id before calling TRPC.

---

## 5) Create payload fields (model specific)

Required fields:
- space (required)
- month (required)
- currency (required)
- incomeTotal (required)
- outflowTotal (required)
- netTotal (required)
- generatedAt (required)
- status (required)

Optional fields:
- transferTotal
- cashAvailable
- recurringTotal
- businessSpendTotal
- personalSpendTotal
- notes

---

## 6) Core Methods

### create(payload)
- TRPC endpoint: `monthlySnapshot.create`

### update(id, payload)
- TRPC endpoint: `monthlySnapshot.update`

### delete(id)
- TRPC endpoint: `monthlySnapshot.delete`

### get(id, resourceKey)
- TRPC endpoint: `monthlySnapshot.resource`
- Known resource keys: Admin

---

## 7) Taxonomy Helper

Available taxonomies:
None

Endpoints:
- `monthlySnapshot.<taxonomy>.createTaxonomy`
- `monthlySnapshot.<taxonomy>.addTerm`
- `monthlySnapshot.<taxonomy>.removeTerm`
- `monthlySnapshot.<taxonomy>.attach`
- `monthlySnapshot.<taxonomy>.detach`
- `monthlySnapshot.<taxonomy>.getTerms`
- `monthlySnapshot.<taxonomy>.getRecordTerms`

---

## 8) Subtable Helper

Subtables under this model:
None

Endpoints:
- `monthlySnapshot.subtables.<child>.create`
- `monthlySnapshot.subtables.<child>.update`
- `monthlySnapshot.subtables.<child>.delete`
- `monthlySnapshot.subtables.<child>.get`
- `monthlySnapshot.subtables.<child>.list` (submany only)

This model is a subtable: no

---

## 9) Relation Helper

Relations for this model:
None

Endpoints:
- `monthlySnapshot.relations.<relation>.attach`
- `monthlySnapshot.relations.<relation>.detach`
- `monthlySnapshot.relations.<relation>.list`

---

## 10) Typesense Helper

Typesense enabled: no

Endpoints (if enabled):
- `monthlySnapshot.typesense.resource`
- `monthlySnapshot.typesense.list`
- `monthlySnapshot.typesense.refresh`
- `monthlySnapshot.typesense.count`
- `monthlySnapshot.typesense.collection`

---

## 11) TRPC Router Location

Generated router:
- `server/trpc/routers/generated/monthlySnapshot.ts`

Wrapper router:
- `server/trpc/routers/monthlySnapshot.ts`

---

End of document.
